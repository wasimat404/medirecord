apiVersion: v1
kind: Service
metadata:
  name: medirecord
  labels:
    app: medirecord
spec:
  type: NodePort
  selector:
    app: medirecord
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
